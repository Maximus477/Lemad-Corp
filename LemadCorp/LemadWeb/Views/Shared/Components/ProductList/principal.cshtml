@model IEnumerable<Principals>

@if (!ViewBag.Verification) {
    <p>No principals availables!</p>
}
else {
    @foreach (Principals item in Model)
    {
        <li class="list-group-item">
            @item.FirstName @item.LastName
        </li>
    }
}

@model IEnumerable<RaceEngineers>

@if (!ViewBag.Verification) {
    <p>No mechanics avaiables!</p>
}
else {
    @foreach (RaceEngineers item in Model)
    {
        <li class="list-group-item">
            @item.FirstName @item.LastName
        </li>
    }
}

@model IEnumerable<PowerUnits>

@if (!ViewBag.Verification) {
    <p>No Power units availables!</p>
}
else {
    @foreach (PowerUnits item in Model)
    {
        <li class="list-group-item">
            @item.Name
        </li>
    }
}
